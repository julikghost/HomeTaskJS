const km = prompt('Enter value you want to convert', '');

alert (convertKmToMiles(km) + ' miles');

document.write(km + '  kms = ' + convertKmToMiles(km) + ' miles');

const numbers = alert('Fill in array');
  

alert( sumInput() );


document.write ("Sum of the numbers is: " + sumInput());
